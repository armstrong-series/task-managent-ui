interface ToastrOptions {
    closeButton?: boolean;
    progressBar?: boolean;
    positionClass?: string;
    timeOut?: number;         
    extendedTimeOut?: number; 
    hideDuration?: number;    
    showDuration?: number;    
   
  }